import React from 'react'

const Modal = () => {
  return (
    <>
   {/* /* {isEditing ? (
        <tr key={item.id}>
        <td>{item.id}</td>
        <td>
          <input
            type="text"
            className="form=control "
            onChange={changeHandler}
          />
        </td>
        <td>
          <span>
            <button className="btn btn-primary me-5 " onClick={cancelHandler}>
              cancel
            </button>
            <button className="btn btn-success ms-5 " onClick={saveHandler}>
              save
            </button>
          </span>
        </td>
      </tr>
      ) : */
    }
    </>
  )
}

export default Modal